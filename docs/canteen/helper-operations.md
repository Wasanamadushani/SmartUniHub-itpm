# Helper Operations Notes

Helper mode in the canteen flow depends on a few user settings:

- helper status enabled
- service charge configured
- active profile data available

Operational reminders:

- only active helpers should send offers
- helper confirmation should happen before pickup
- live location updates should be sent after assignment
- payment confirmation should happen only after cash is received
